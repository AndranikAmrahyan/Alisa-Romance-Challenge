# 🎮 Telegram Bot "Влюби в себя Алису"

Телеграм бот с AI персонажем, где игроки пытаются влюбить в себя токсичную девушку Алису. Настоящий челлендж с тремя уровнями сложности!

## 🌟 Особенности

- **AI на базе Groq API** (бесплатный, модель llama-3.3-70b-versatile)
- **Три уровня сложности** (Easy, Medium, Hard) с разными правилами и длительностью
- **Токсичный персонаж** с острым языком и матами (зависит от сложности)
- **Система игнора** - Алиса может игнорировать грубых участников, но прощает после извинений
- **SQLite база данных** для хранения сообщений и игровых сессий
- **Автоматическое завершение игры** по времени или неактивности
- **Мгновенная победа** - если Алиса влюбляется прямо в ответе
- **Работает в группах** Telegram
- **Деплой на Render** (бесплатный тариф)

## 🎯 Уровни сложности

### 😇 Легкая (Easy)
- **Длительность:** До 15 минут
- **Характер:** Дружелюбная, но с характером
- **Маты:** Умеренно
- **Победа:** Легко влюбляется (5-10 минут нормального общения)
- **Идеально для:** Начинающих и веселой игры

### 😏 Средняя (Medium)
- **Длительность:** До 30 минут
- **Характер:** Токсичная и дерзкая
- **Маты:** Часто
- **Победа:** Требует 15-20 минут качественного общения
- **Идеально для:** Тех, кто хочет баланс между сложностью и весельем

### 😈 Сложная (Hard)
- **Длительность:** До 1 часа
- **Характер:** Очень токсичная, неприступная
- **Маты:** Свободно использует
- **Победа:** Очень сложно, требует оригинальности и харизмы
- **Важно:** Сначала ПОЗНАКОМЬСЯ, потом подкатывай!
- **Идеально для:** Настоящих ценителей челленджа

## 🚀 Установка

### 1. Получить API ключи

**Telegram Bot:**
1. Напишите [@BotFather](https://t.me/BotFather)
2. Отправьте `/newbot`
3. Следуйте инструкциям
4. Скопируйте токен

**Groq API (бесплатный):**
1. Зарегистрируйтесь на [console.groq.com](https://console.groq.com)
2. Создайте API ключ
3. Лимиты: 30 запросов/минуту, 14,400 запросов/день (достаточно!)

### 2. Настройка проекта

```bash
# Клонируйте или создайте папку проекта
mkdir telegram-bot-alisa
cd telegram-bot-alisa

# Создайте все файлы из артефактов:
# - main.py
# - config.py
# - database.py
# - ai_handler.py
# - infrastructure.py
# - requirements.txt
# - .env

# Создайте .env файл
nano .env
```

**Содержимое .env:**
```env
TELEGRAM_BOT_TOKEN=your_telegram_bot_token_here
GROQ_API_KEY=your_groq_api_key_here
RENDER_APP_URL=https://your-app.onrender.com
BACKUP_CHAT_ID=your_telegram_chat_id_for_backups
```

### 3. Локальный запуск (для тестирования)

```bash
# Установите зависимости
pip install -r requirements.txt

# Запустите бота
python main.py
```

### 4. Деплой на Render

1. Создайте аккаунт на [render.com](https://render.com)
2. Создайте новый **Web Service**
3. Подключите ваш GitHub репозиторий или загрузите файлы
4. Настройки:
   - **Runtime:** Python 3
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python main.py`
5. Добавьте Environment Variables:
   - `TELEGRAM_BOT_TOKEN`
   - `GROQ_API_KEY`
   - `RENDER_APP_URL` (добавите после деплоя)
   - `BACKUP_CHAT_ID` (опционально)
6. Deploy!
7. После деплоя скопируйте URL (типа `https://your-app.onrender.com`) и добавьте в `RENDER_APP_URL`

## 🎮 Как играть

### Запуск игры

Добавьте бота в группу Telegram и запустите игру одним из способов:

**Командами:**
- `/start` - откроет меню выбора сложности
- `/alisa` - откроет меню выбора сложности
- `/help` - покажет справку

**Текстовыми триггерами:**
- "Алиса приходи"
- "Алиса давай играть"
- "Алиса начнем"
- "Алиса го играть"
- "Алиса старт"
- "Алиса выходи"

### Выбор сложности

После запуска появятся кнопки для выбора уровня:
- 😇 **Легкая (Easy)** - для новичков
- 😏 **Средняя (Medium)** - для опытных
- 😈 **Сложная (Hard)** - для профи

### Общение с Алисой

Алиса реагирует на сообщения тремя способами:

1. **Reply (ответ)** - ответьте на её сообщение
2. **Команда** - используйте `/say ваш_текст`
3. **Упоминание** - напишите "Алиса" в тексте сообщения

### Правила игры

**Общие для всех уровней:**
- Алиса может обидеться и предупредить перед игнором
- Если продолжите хамить - получите игнор (но можно извиниться!)
- Молчание больше 5 минут = конец игры
- Игнор одного участника не влияет на общение с другими

**Специфичные для Hard:**
- ОБЯЗАТЕЛЬНО сначала познакомьтесь, потом подкатывайте
- Избегайте шаблонных фраз типа "привет красотка"
- Будьте оригинальными и креативными
- Умейте шутить и не бойтесь быть дерзкими

### Победа

Алиса объявит победителя двумя способами:

1. **Мгновенная победа** - прямо в ответе скажет "Я в тебя влюбилась. Хочу быть с тобой ❤️"
2. **По таймеру** - AI проанализирует всех участников и выберет лучшего

Если никто не впечатлит за отведённое время - игра закончится без победителя.

## ⚙️ Настройка персонажа

Все настройки в `config.py`:

### Базовые настройки
```python
BOT_NAME = "Алиса"           # Имя девушки
BOT_AGE = "взрослая"         # Возраст (не говорит точно)
BOT_COUNTRY = "Россия"       # Страна
BOT_CITY = "Москвы"          # Город
COMMAND_PREFIX = "/say"      # Команда для обращения
```

### Триггеры запуска
```python
START_TRIGGERS = [
    "алиса приходи",
    "алиса давай играть",
    "алиса начнем",
    "алиса го играть",
    "алиса старт",
    "алиса выходи"
]
```

### Тайминги игры
```python
MIN_GAME_DURATION = 300              # 5 минут минимум
MAX_GAME_DURATION_EASY = 900         # 15 минут для Easy
MAX_GAME_DURATION_MEDIUM = 1800      # 30 минут для Medium
MAX_GAME_DURATION_HARD = 3600        # 1 час для Hard
CHECK_INTERVAL = 300                 # Проверка каждые 5 минут
```

### AI настройки
```python
AI_MODEL = "llama-3.3-70b-versatile"  # Groq модель
AI_MAX_TOKENS = 500                   # Максимум токенов
AI_TEMPERATURE = 0.95                 # Креативность (0.0-1.0)
```

## 🎭 Промпты для каждой сложности

Каждый уровень имеет свой промпт в `ai_handler.py`:

- **`prompt_hard`** - для сложного режима (неприступная, требовательная)
- **`prompt_medium`** - для среднего режима (токсичная, но прощает)
- **`prompt_easy`** - для легкого режима (дружелюбная с характером)

Вы можете изменить их под свои нужды!

## 📊 Лимиты API

**Groq (бесплатный):**
- 30 запросов/минуту
- 14,400 запросов/день
- Примерно 10 пользователей могут активно играть 2 часа без проблем

**Render (бесплатный):**
- 750 часов/месяц
- Засыпает после 15 минут неактивности
- Self-ping решает проблему (каждые 10 минут пингует себя)

## 🛠️ Структура файлов

```
telegram-bot-alisa/
├── main.py              # Основной файл бота с handlers
├── config.py            # Конфигурация и настройки
├── database.py          # Работа с SQLite (сессии, сообщения)
├── ai_handler.py        # Общение с Groq API (3 промпта)
├── infrastructure.py    # Flask сервер и задачи (ping, backup)
├── requirements.txt     # Зависимости Python
├── .env                 # Переменные окружения (НЕ коммитить!)
├── .python-version      # Версия Python (3.12.0)
├── bot_data.db          # SQLite база (создаётся автоматически)
└── README.md            # Эта документация
```

## 🔧 Технические детали

### База данных

**Таблицы:**

1. `game_sessions` - игровые сессии
   - `chat_id` - ID чата
   - `is_active` - активна ли игра
   - `difficulty` - уровень сложности
   - `started_at` - время начала
   - `ended_at` - время окончания
   - `winner_user_id` - ID победителя
   - `winner_name` - имя победителя

2. `participant_messages` - сообщения участников
   - `chat_id` - ID чата
   - `user_id` - ID пользователя
   - `username` - username
   - `first_name` - имя
   - `message` - текст сообщения
   - `timestamp` - время отправки

3. `conversation_history` - история разговора для AI
   - `chat_id` - ID чата
   - `role` - роль (user/assistant)
   - `content` - содержимое
   - `timestamp` - время

### Фоновые задачи

- **check_game_progress** - проверяет каждую активную игру каждые 5 минут:
  - Неактивность (молчание > 5 минут)
  - Превышение максимального времени
  - Проверка на победителя
  
- **self_ping** - пингует Render каждые 10 минут, чтобы не заснул

- **backup_database** (опционально) - бэкапит базу каждые 24 часа

### AI Decision Making

AI принимает решение о победителе на основе:
- Оригинальности сообщений
- Количества сообщений
- Качества общения
- Соблюдения правил (знакомство перед подкатом для Hard)
- Юмора и харизмы
- Романтики и искренности

## 🐛 Решение проблем

### Бот не отвечает
- ✅ Проверьте что бот добавлен в группу
- ✅ Убедитесь что игра запущена (`/start`)
- ✅ Проверьте что обращаетесь правильно (reply, `/say`, или "Алиса")
- ✅ Проверьте логи в Render

### AI не работает
- ✅ Проверьте GROQ_API_KEY в .env
- ✅ Проверьте лимиты на console.groq.com
- ✅ Посмотрите логи - там будут ошибки API

### Render засыпает
- ✅ Убедитесь что RENDER_APP_URL заполнен правильно
- ✅ Self-ping должен работать (проверьте логи)
- ✅ Проверьте что Flask сервер запустился (порт 10000)

### База данных
- ✅ `bot_data.db` создаётся автоматически при первом запуске
- ✅ Если нужно сбросить - просто удалите файл
- ✅ Миграция difficulty добавляется автоматически для старых БД

### Игра не заканчивается
- ✅ Проверьте что CHECK_INTERVAL и MAX_GAME_DURATION настроены правильно
- ✅ Посмотрите логи - там будут сообщения о проверках
- ✅ Убедитесь что фоновая задача не упала (restart бота)

## 🎨 Кастомизация

### Изменить личность Алисы

Отредактируйте промпты в `ai_handler.py`:

```python
self.prompt_hard = f"""Ты — {config.BOT_NAME}, ...
# Здесь описание личности
# Что любит/ненавидит
# Правила поведения
# Критерии влюблённости
"""
```

### Добавить новые команды запуска

В `config.py`:

```python
START_TRIGGERS = [
    "алиса приходи",
    "ваша новая фраза",
    # ...
]
```

### Изменить длительность игры

В `config.py`:

```python
MAX_GAME_DURATION_HARD = 7200  # 2 часа вместо 1
MAX_GAME_DURATION_EASY = 600   # 10 минут вместо 15
```

### Добавить свой уровень сложности

1. Добавьте константу в `config.py`
2. Создайте новый промпт в `ai_handler.py`
3. Добавьте кнопку в `main.py` в функции `start()`
4. Обновите логику в `get_max_game_duration()`

## 📈 Статистика и мониторинг

### Логи

Все важные события логируются:
- Запуск/завершение игры
- Обработка сообщений
- Ошибки API
- Решения AI

Посмотреть логи на Render: Dashboard → Logs

### База данных

Можно скачать `bot_data.db` и открыть в SQLite браузере для анализа:
- Кто чаще играет
- Сколько длятся игры
- Кто побеждает
- Популярные сообщения

## 🤝 Вклад в проект

Идеи для улучшения:
- [ ] Добавить статистику побед каждого игрока
- [ ] Рейтинговая система
- [ ] Достижения (ачивки)
- [ ] Мультиязычность
- [ ] Больше уровней сложности
- [ ] Веб-интерфейс для статистики
- [ ] Кастомные персонажи
- [ ] Голосовые сообщения (TTS)

## 📝 Лицензия

Свободное использование для личных и образовательных целей.

## 🎉 Удачи!

Попробуйте влюбить в себя Алису - это не так просто! 😈

**Совет:** Начните с Easy, потом попробуйте Medium, и только смелые идут на Hard!

---

*Made with ❤️*